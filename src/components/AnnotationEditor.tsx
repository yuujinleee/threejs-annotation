import { TAnnotationEditor } from "../types/type";

export default function AnnotationEditor({
  id,
  content,
  username,
  status,
}: TAnnotationEditor) {
  return <>I am Annotation Editor tsx</>;
}
